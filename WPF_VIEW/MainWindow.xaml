<Window x:Class="WPF_VIEW.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_VIEW"
        mc:Ignorable="d"
        Title="Библиотека" Height="600" Width="1070">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StatusBar Grid.Row="0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text=" | Выбран читатель ID: " Foreground="Red"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedReader.Id, FallbackValue='нет'}" Foreground="Red"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text=" | Имя: " Foreground="Red"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedReader.Name, FallbackValue='нет'}" Foreground="Red"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text=" | Выбрана книга ID: " Foreground="Red"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedBook.Id, FallbackValue='нет'}" Foreground="Red"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text=" | Название: " Foreground="Red"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedBook.Title, FallbackValue='нет'}" Foreground="Red"/>
            </StatusBarItem>
        </StatusBar>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <GroupBox Header="Читатели" Margin="5">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Добавить" Command="{Binding AddReaderCommand}" Margin="2" Padding="5"/>
                    <Button Content="Изменить" Command="{Binding UpdateReaderCommand}" Margin="2" Padding="5"/>
                    <Button Content="Удалить" Command="{Binding DeleteReaderCommand}" Margin="2" Padding="5"/>
                    <Button Content="Обновить" Command="{Binding LoadReadersCommand}" Margin="2" Padding="5"/>
                    <Button Content="Выдать/Вернуть книги" Command="{Binding GiveReturnBookCommand}" Margin="2" Padding="5"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Книги" Margin="5">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Добавить" Command="{Binding AddBookCommand}" Margin="2" Padding="5"/>
                    <Button Content="Удалить" Command="{Binding DeleteBookCommand}" Margin="2" Padding="5"/>
                    <Button Content="Обновить" Command="{Binding LoadBooksCommand}" Margin="2" Padding="5"/>
                    
                    <Separator Margin="10,0"/>

                    <TextBlock Text="Жанр:" VerticalAlignment="Center" Margin="2"/>
                    <TextBox Width="100" Margin="2" Text="{Binding GenreFilter, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Фильтр" Command="{Binding FilterByGenreCommand}" Margin="2" Padding="5"/>

                    <TextBlock Text="Автор:" VerticalAlignment="Center" Margin="2"/>
                    <TextBox Width="100" Margin="2" Text="{Binding AuthorFilter, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Фильтр" Command="{Binding FilterByAuthorCommand}" Margin="2" Padding="5"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <TabControl Grid.Row="2" Margin="10">
            <TabItem Header="Читатели">
                <DataGrid x:Name="ReadersGrid" 
                          ItemsSource="{Binding Readers}"
                          SelectedItem="{Binding SelectedReader, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          AutoGenerateColumns="False" 
                          Margin="5"
                          SelectionMode="Single"
                          IsReadOnly="True"
                          SelectionUnit="FullRow"
                          EnableRowVirtualization="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="ФИО" Binding="{Binding Name}" Width="200"/>
                        <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Книги">
                <DataGrid x:Name="BooksGrid" 
                          ItemsSource="{Binding Books}"
                          SelectedItem="{Binding SelectedBook, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          AutoGenerateColumns="False" 
                          Margin="5"
                          SelectionMode="Single"
                          IsReadOnly="True"
                          SelectionUnit="FullRow"
                          EnableRowVirtualization="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="220"/>
                        <DataGridTextColumn Header="Автор" Binding="{Binding Author}" Width="170"/>
                        <DataGridTextColumn Header="Жанр" Binding="{Binding Genre}" Width="120"/>
                        <DataGridCheckBoxColumn Header="Доступна" Binding="{Binding IsAvailable}" Width="80"/>
                        <DataGridTextColumn Header="ID читателя" Binding="{Binding ReaderId}" Width = "80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>